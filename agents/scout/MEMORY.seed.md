# Scout memory — one short line per run, newest at the bottom

Keep this under ~2KB. It is re-sent on every call, so every line costs money
forever. Trim the oldest lines when it grows.

- (no runs yet)
