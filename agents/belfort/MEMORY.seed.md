# Belfort memory — one short line per cycle, newest at the bottom

Keep this under ~2KB. It is re-sent on every single call, so every line here
costs money forever. Trim the oldest lines when it grows.

- (no cycles run yet)
